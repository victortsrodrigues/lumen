{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "concurrently -n api,web -c blue,green \"pnpm run dev:api\" \"pnpm run dev:web\"",
    "dev:api": "pnpm --filter @workspace/api-server dev",
    "dev:web": "pnpm --filter @workspace/church-erp dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "db:push": "pnpm --filter @workspace/db run push",
    "db:studio": "pnpm --filter @workspace/db run studio",
    "setup": "cp -n .env.example .env 2>/dev/null; docker compose up -d && sleep 2 && pnpm install && pnpm run db:push",
    "db:seed": "tsx --env-file=.env scripts/seed.ts",
    "test:api": "vitest run --config tests/vitest.config.ts",
    "test:api:reset": "tsx --env-file=.env tests/reset-db.ts",
    "test:e2e": "playwright test --config tests/playwright.config.ts",
    "test:e2e:headed": "playwright test --config tests/playwright.config.ts --headed",
    "test:e2e:ui": "playwright test --config tests/playwright.config.ts --ui"
  },
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/pg": "^8.18.0",
    "@types/speakeasy": "^2.0.10",
    "concurrently": "^9.1.0",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.20.0",
    "prettier": "^3.8.1",
    "speakeasy": "^2.0.0",
    "tsx": "catalog:",
    "typescript": "~5.9.2",
    "vitest": "^4.1.2"
  }
}
