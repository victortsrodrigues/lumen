services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: church_erp
      POSTGRES_PASSWORD: church_erp
      POSTGRES_DB: church_erp
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
